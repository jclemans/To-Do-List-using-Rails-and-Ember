<div class="container">
  <hr>
  <div class="row">
    <div class="col-sm-5">
      {{#link-to 'new_task' class='btn-sm btn-primary'}}moar things!{{/link-to}}
      <br><br>

      <ol>
        {{#each}}
        <li>{{#link-to 'task' this}}{{name}}{{/link-to}}</li>
        {{/each}}
      </ol>

    </div>

    <div class="col-sm-6">
      {{outlet}}
    </div>

  </div>
</div>


