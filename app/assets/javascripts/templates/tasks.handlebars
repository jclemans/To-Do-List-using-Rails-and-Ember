<div class="container">
  <hr>
  <div class="row">
    <div class="col-sm-5">
      {{#link-to 'new_task' class='btn-sm btn-success'}}moar things!{{/link-to}}
      <br><br>

      <ol id="task-list">
        {{#each}}
        <li>{{#link-to 'task' this}}{{name}}{{/link-to}}</li>
        {{/each}}
      </ol>

    </div>

    <div class="col-sm-6">
      {{outlet}}
    </div>

  </div>
</div>


